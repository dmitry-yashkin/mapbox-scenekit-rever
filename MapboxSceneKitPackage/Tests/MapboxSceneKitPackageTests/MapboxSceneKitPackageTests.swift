import XCTest
@testable import MapboxSceneKitPackage

final class MapboxSceneKitPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MapboxSceneKitPackage().text, "Hello, World!")
    }
}
